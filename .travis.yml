language: python

python:
    - "3.5"

# install dependencies
install:
  - cd backend
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt

script:
  - pytest

notifications:
  email: false
